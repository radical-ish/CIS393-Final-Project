# Random Fact Generator

A MySQL database is required for this project to function. An sql file containing some mock up data should be provided in this folder. After setting up the database and installing all dependencies, start the project by running nodemon on index.js.
